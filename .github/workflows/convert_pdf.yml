---
name: Convert pdfs to markdown
run-name: Convert PDFs to markdown

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  convert-pdf:
    permissions:
      contents: read # To enable linting of files in repo
      statuses: write # To report GitHub Actions status checks      
    runs-on: ubuntu-latest

    steps:
     - name: Checkout code
       uses: actions/checkout@v4.2.2

     - name: Configure Python
       uses: actions/setup-python@v5.4.0
       with:
         python-version: '3.11'

     - name: Cache pip
       uses: actions/cache@v3.5.0
       with:
         path: ~/.cache/pip
         key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
         restore-keys: |
           ${{ runner.os }}-pip-

     - name: Install packages
       run: | 
         pip install marker-pdf

     - name: Convert PDF to markdown
       run: |
         mkdir -p output
         marker_single input/msp430_user_guide.pdf --output_format markdown --output_dir output

     - name: Upload converted files
       uses: actions/upload-artifact@v3.1.2
       with:
         name: converted-markdown
         path: output
